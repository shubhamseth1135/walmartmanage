@{
    Layout = "~/Views/Shared/_PublicLayout.cshtml";
}

@{
    ViewBag.Title = "employeeLogin";
}

<h2>EmployeeLogin</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>storeemployees</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            <div class="col-md-2">
                @Html.Label("ID")
            </div>
            <div class="col-md-10">
                @Html.Editor("id", new { htmlAttributes = new { @class = "form-control", placeholder = "Enter id" } })
                @Html.ValidationMessage("id", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-2">
                @Html.Label("Password")
            </div>
            <div class="col-md-10">
                @Html.Editor("password", new { htmlAttributes = new { @class = "form-control", @type = "password", placeholder = "Enter Password" } })
                @Html.ValidationMessage("password", new { @class = "text-danger" })
            </div>
        </div>

        <input type="submit" value="Login" class="btn btn-success btn-block" />
    </div>

}


